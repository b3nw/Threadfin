{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Threadfin",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "args": ["-dev", "-config", "${workspaceFolder}/.threadfin-dev"],
            "preLaunchTask": "tsc build"
        }
    ]
}
